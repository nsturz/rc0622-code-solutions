/* exported getFirstChar */

/*
we need to access the first character of the string, indexed at [0]
we then need to return it from the functions code block
*/
function getFirstChar(string) {
  return string[0];
}
