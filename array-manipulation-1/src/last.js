/* exported last */
/*

in this one, we need to return the last element of the array,
indexed at array.length-1 from our function's code block
*/

function last(array) {
  return array[array.length - 1];
}
